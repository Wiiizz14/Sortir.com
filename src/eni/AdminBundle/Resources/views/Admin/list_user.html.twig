{% extends 'base.html.twig' %}

{% block title %}Gestion Du Site{% endblock %}
{% block pagetitle %}Gestion du site{% endblock %}

{% block body %}
    <p><a href="{{ path('admin_createUser') }}">Ajouter un participant</a></p>
    {%  for participant in listeParticipants %}
    <div>
        <p>{{ participant.username }}</p>
    </div>
    <a href="{{  path('admin_detailUser', {"participant": participant.id}) }}">detail</a>
    <a href="{{  path('admin_updateUser', {"participant": participant.id}) }}">modifier</a>
    <a href="{{ path('admin_deleteUser', {"participant": participant.id}) }}">Supprimer</a>
    {%  else %}
        <p>Aucun participants... :(</p>
    {% endfor %}

{% endblock %}


